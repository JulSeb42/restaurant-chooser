import { commands } from "@uiw/react-md-editor"

const markdownInputConfig = [
    commands.bold,
    commands.italic,
    commands.quote,
    commands.unorderedListCommand,
    commands.orderedListCommand,
    commands.title3,
    commands.title4,
]

export default markdownInputConfig
